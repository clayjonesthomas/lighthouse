
const defaultAdminState = {
  postTitleValue: '',
  selectedShops: [],
  isImportant: "False"
}

export function admin(state = defaultAdminState, action) {
  switch (action.type) {
    default:
      return state
  }
}
